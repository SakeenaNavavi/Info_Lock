{
    "name": "infolock",
    "version": "1.0.0",
    "engines": {
      "node": "18.17.1"
    },
    "scripts": {
      "start": "node backend/server.js",
      "server": "cd backend && npm run dev",
      "client": "cd frontend && npm start",
      "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
      "build-client": "cd frontend && npm run build",
      "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run install-all && npm run build-client"
    },
    "dependencies": {
      "concurrently": "^8.2.2"
    },
    "devDependencies": {
      "nodemon": "^3.1.7"
    }
  }